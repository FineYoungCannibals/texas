services:
  texasmysql:
    image: mysql:latest
    restart: none
    volumes:
      - /opt/docker/texas/mysql:/var/lib/mysql
    networks:
      - dev_lab
    entrypoint: ["mysqld"]
networks:
  lab_dev:
    external: true
